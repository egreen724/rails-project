<%= form_for @trip do |f| %>
<br>
  Activity Name: <%= f.collection_select :activity_id, Activity.all, :id, :name %> <br>
  <%= f.hidden_field :user_id, :value => @current_user.id %>
  <br>
  <br>
  Would you like to add this activity to your To Do List or Log a Trip? <br>
  <%= f.select :taken, [["To Do List", false], ["Log a Trip", true]]%>
  <br>
  <br>
  Complete these fields if you are logging a trip: <br>
    Date: <%= f.text_field :date %> <br>
    Time (in minutes): <%= f.text_field :time_ellapsed %> <br>
    User Comments: <%= f.text_field :comment %> <br>

    <%= f.submit "Submit" %>
  <% end %>

    <br>
